# 思研阁小程序 UI 自动化测试 使用指南

本套测试框架基于微信官方的 `miniprogram-automator` 和 `Jest` 搭建，旨在实现核心业务流程（登录、排课、权限控制、数据校验）的自动化回归。

## 1. 运行前置条件
在执行自动化脚本前，请务必完成以下配置：
1.  **开启服务端口**：
    - 打开【微信开发者工具】。
    - 进入【设置】 -> 【安全设置】。
    - 将【服务端口】选项设置为：**开启**。
2.  **安装 Node.js 环境**：建议使用 Node.js 16.x 及以上版本。

## 2. 快速开始
在项目根目录下，通过终端执行以下命令：

```bash
# 1. 安装依赖 (首次执行)
npm install

# 2. 运行所有测试用例
npm test
```

执行后，微信开发者工具将自动启动并模拟用户操作。

## 3. 测试套件说明
测试脚本存放于项目根目录的 `test/` 文件夹中：
- `login.test.js`：验证登录异常提示（账号/密码错误）及成功登录跳转。
- `schedule.test.js`：验证老师与学生视角的 UI 差异（权限隔离）。
- `name_unique.test.js`：验证跨角色的姓名唯一性硬性拦截逻辑。
- `utils.js`：封装了通用的 `login` 和 `initApp` 助手函数。

## 4. 如何扩展测试
您可以参考已有的 `.test.js` 文件编写新的场景。
- **获取元素**：使用 `page.$(selector)` 或 `page.$$(selector)`。
- **模拟操作**：使用 `element.tap()` 进行点击，或 `element.input(value)` 填写表单。
- **状态验证**：使用 `page.data()` 获取当前页面的 Data 状态并进行断言。

## 5. 注意事项
- 由于云函数加载可能存在延迟，测试用例默认设置了较长的超时时间（60秒）。
- 若出现连接失败，请检查微信开发者工具是否处于最新版本，并确认服务端口已开启。

---
*质量是教学服务的基石。通过自动化测试，思研阁系统每一天的迭代都更加稳健。*
