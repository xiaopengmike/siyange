# 思研阁老师排课系统 - 云开发部署指南

## 📋 项目结构

```
思研阁 老师排课小程序/
├── miniprogram/           # 小程序前端代码
│   ├── pages/            # 页面
│   │   ├── login/        # 登录页
│   │   ├── schedule/     # 课表页
│   │   └── course-edit/  # 课程编辑页
│   ├── components/       # 组件
│   │   └── schedule-grid/  # 课表网格组件
│   ├── utils/            # 工具函数
│   │   ├── db.js         # 数据库操作
│   │   └── dateUtil.js   # 日期工具
│   └── config/           # 配置
│       └── cloud.js      # 云开发配置 ⬅️ 需要配置环境ID
│
├── cloudfunctions/        # 云函数
│   ├── initData/         # 初始化老师数据
│   └── courseManager/    # 课程管理（增删改查）
│
└── project.config.json    # 项目配置
```

## 🚀 部署步骤

### 第一步：开通云开发

1. 打开**微信开发者工具**
2. 点击顶部菜单栏的 **「云开发」** 按钮
3. 如未开通，点击 **「开通」** 按钮
4. 选择 **付费模式**（可以选择按量付费，有免费额度）
5. 创建环境，记录下 **环境ID**

### 第二步：配置环境ID

打开 `miniprogram/config/cloud.js`，填入您的云开发环境ID：

```javascript
module.exports = {
  envId: 'your-env-id-xxx'  // ← 替换为您的环境ID
}
```

### 第三步：创建数据库集合

1. 在云开发控制台中，点击左侧 **「数据库」**
2. 点击 **「+ 添加集合」**，创建以下集合：
   - `teachers` - 老师信息表
   - `courses` - 课程信息表

### 第四步：设置集合权限

对于每个集合，点击集合名称 → 点击 **「权限设置」** → 选择 **「所有用户可读，仅创建者可读写」** 或 **「所有用户可读写」**（开发阶段推荐）

### 第五步：部署云函数

1. 在微信开发者工具中，找到左侧 `cloudfunctions` 目录
2. 右键点击 `initData` 文件夹 → 选择 **「上传并部署：云端安装依赖（不上传node_modules）」**
3. 右键点击 `courseManager` 文件夹 → 同样操作

### 第六步：编译运行

点击微信开发者工具顶部的 **「编译」** 按钮，即可运行小程序！

## 📊 数据结构

### teachers 集合

| 字段 | 类型 | 说明 |
|------|------|------|
| teacherId | string | 老师唯一标识 |
| name | string | 老师姓名 |
| color | string | 显示颜色（十六进制） |
| createTime | date | 创建时间 |

### courses 集合

| 字段 | 类型 | 说明 |
|------|------|------|
| teacherId | string | 老师ID |
| date | string | 日期（YYYY-MM-DD） |
| startTime | string | 开始时间（HH:mm） |
| endTime | string | 结束时间（HH:mm） |
| courseName | string | 课程名称 |
| studentName | string | 学生姓名 |
| notes | string | 备注 |
| color | string | 显示颜色 |
| createTime | date | 创建时间 |
| updateTime | date | 更新时间 |

## 🔧 云函数说明

### initData

初始化老师数据，首次运行时自动创建默认老师：
- Judy老师
- 某英语老师
- 王老师
- 李老师

### courseManager

课程管理云函数，支持以下操作：
- `add` - 添加课程
- `update` - 更新课程
- `delete` - 删除课程
- `getByTeacher` - 获取老师的课程
- `getAll` - 获取所有课程
- `checkConflict` - 检查时间冲突

## ❓ 常见问题

### Q: 提示"请使用 2.2.3 或以上的基础库"

A: 在开发者工具右下角，点击 **「详情」** → **「本地设置」** → 将调试基础库版本设置为 2.2.3 或以上。

### Q: 云函数调用失败

A: 检查以下几点：
1. 云开发环境ID是否正确配置
2. 云函数是否已上传部署
3. 数据库集合是否已创建
4. 集合权限是否正确设置

### Q: 老师数据没有显示

A: 尝试在云开发控制台手动调用 `initData` 云函数，或检查控制台日志。

## 📝 快速添加老师

在云开发控制台的 `teachers` 集合中手动添加记录：

```json
{
  "teacherId": "teacher_005",
  "name": "新老师",
  "color": "#E91E63"
}
```
